package com.project.helpcircle.domain.model

/** A community member's coarse standing, shared with peers in place of their raw IA_ind score. */
enum class MemberStatus {
    OK,
    AT_RISK,
    CRISIS
}

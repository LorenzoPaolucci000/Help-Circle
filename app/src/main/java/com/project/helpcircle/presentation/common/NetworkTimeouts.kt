package com.project.helpcircle.presentation.common

/** Shared timeout/messaging for any ViewModel action that wraps a network call in `withTimeout`. */
const val NETWORK_TIMEOUT_MILLIS = 15_000L
const val SLOW_CONNECTION_MESSAGE = "Connection is slow, tap to try again"
